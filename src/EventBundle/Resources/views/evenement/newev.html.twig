
{% extends 'base.html.twig' %}
{#{% block header %} {% endblock %}#}
{% block container %}
    <h1>Ajouter un Evenement </h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="ajouter" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('afficheev') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
